---
name: 零件杂货铺 (Grocery Workshop)
description: Agent 零件管理与 Skill 自动组装，并内置 WorkBuddy 管控台（console.html）。安装后调用即可在浏览器打开统一控制台，管理已装 Skill（健康度 / 升级 / 删除）、存储空间、历史对话与设置；同时保留零件库检索、冲突二选一、依赖补全、自动拼装 Skill 的能力，拼装记录直接在控制台「Skill开发 / 任务拆解 / 组件管理」中查看。
agent_created: true
---

# 零件杂货铺 (Grocery Workshop)

## 一、产品是什么

**零件杂货铺**是一个**本地化的 Agent 工作台**，把两件原本分散的事合到一处：

1. **管控台（主交付物）** —— 一张零依赖的浏览器控制台，统一管理你的 WorkBuddy 环境：已装 Skill 的健康度 / 升级 / 删除、存储空间的占用明细与清理、历史对话的浏览与全文检索、自动化任务与运行异常，所有信息在一张「概览驾驶舱」里一屏可见。
2. **零件工坊（引擎）** —— 把已有的 Skill / 文档拆成可复用的「零件」，再按你的模糊需求自动检索、冲突二选一、依赖补全，拼装成一个新 Skill；新 Skill 还能自动拆回零件库，形成「越用越满」的积累闭环。

它的定位不是又一个聊天窗口，而是一台能让你**看清 Agent 由什么构成、并能自己造零件、自己组装新能力**的本地机床。

## 二、产品介绍

- **安装即内置控制台**：作为 WorkBuddy 的一个 Skill 安装后，调用它即打开浏览器控制台，无需额外搭建环境。
- **零外部依赖、纯本地**：前端是单个 HTML 文件，后端是本地轻量服务，所有数据都来自对你本机 WorkBuddy 目录的扫描，没有云端数据库、不上传你的内容。
- **可控、可溯源**：任何删除操作都会先生成总结备份并移入系统回收站（可恢复）；任何一个生成的 Skill 都能回溯到它由哪些零件组成、每个零件被哪些 Skill 用过。
- **三大核心价值**
  - **透明**：一眼看清你的 Agent 资产全景——多少个 Skill、谁偏胖、谁有安全风险、谁和谁功能重叠。
  - **积累**：组装与拆解互为正反过程，零件库随使用自然丰盈，下次组装的选择更多。
  - **可控**：全自动组装 + 完整溯源，过程不黑盒；危险操作有备份与二次确认。

适用人群：想统一管理 Skill / 存储 / 对话的 WorkBuddy 用户；想把零散能力沉淀成可复用零件的开发者；想凭一个想法快速造出新 Skill 的创作者。

## 三、功能介绍

控制台左侧导航共 10 个板块，能力如下：

| 板块 | 说明 |
|---|---|
| **概览** | Agent 资产全景驾驶舱：KPI（已装 Skill 数 / Token 偏胖数 / 近 7·30 天对话数 / 安全警告数 / 功能重叠对数）、健康度环形图、存储分布、Token 消耗 TOP5；并内嵌「生命周期」三栏：功能重叠检测、安全风险扫描、更新检查。 |
| **Skill 管理** | 已装 Skill 明细表：健康度 / 来源 / 安装日期 / 未使用天数 / 使用次数 / Token / 体积 / 安全等级 / 状态。支持筛选（全部 / 风险 / 偏胖 / 高危）、搜索、翻页（每页 10 行）、一键升级、页面内安全删除。 |
| **存储空间** | 占用按「日志 / 会话 / 缓存」分类明细，按风险分四级（放心清理 / 谨慎清理 / Skill 目录 / 不可删除）；提供一键清理，会话类清理会先生成 AI 总结备份再移入回收站。 |
| **Skill开发** | 查看历史生成记录；点「新建 Skill」描述需求 → 自动检索零件 → 组装 → 生成可安装 Skill → 一键「安装」到 WorkBuddy。记录含状态（已生成 / 已安装 / 生成中 / 安装失败）与选用零件数。 |
| **任务拆解** | 已生成 Skill 自动拆解回填零件的进度看板，展示「组装 → 拆解 → 回填」闭环。 |
| **组件管理** | 零件货架：按 6 大类（需求分析 / 方案设计 / 代码开发 / 测试用例 / 依赖工具 / 参考文档）浏览，卡片 / 列表双视图，支持翻页；点零件可看详情与反向关联（被哪些 Skill 使用）。 |
| **对话** | 两个标签页：①「WorkBuddy 会话」—— 左侧按项目 / 会话树浏览，顶部搜索框跨全部历史会话全文检索并高亮命中，点会话查看渲染后的消息；②「与 Skill 对话」—— 选一个已装 Skill 即可实时问答（需先在设置配置 LLM）。 |
| **任务** | 自动化任务列表与运行状态。 |
| **异常日志** | 运行异常与错误采样，便于排查。 |
| **设置** | 主题、后端地址、开发环境信息、LLM、SkillHub 五项配置（详见第六节）。 |

## 四、使用说明

> 本产品的所有操作都在浏览器控制台内完成，不需要手动执行任何命令行。下面以「问答场景 + 界面操作」两种方式说明。

### 4.1 常见问答场景

**问：我想看看我一共装了多少个 Skill，有没有安全风险？**
→ 打开控制台，进入 **概览**。顶部 KPI 直接显示「已装 Skill 数」「Token 偏胖数」「安全警告数」「功能重叠对数」；下方「🛡️ 安全风险」卡片列出高风险的 Skill 及具体发现（危险命令 / 提权 / 动态执行等）；「🧬 功能重叠检测」列出功能重复的 Skill 对与重叠百分比。

**问：某个 Skill 我不想要了，怎么安全删除？**
→ 进入 **Skill 管理**，在表格里找到目标 Skill，点该行右侧的 **删除** 按钮。系统会先生成 AI 总结备份，再把原始目录移入系统回收站（之后可从回收站恢复），不会直接永久抹除。

**问：WorkBuddy 占用了很多空间，怎么清理？**
→ 进入 **存储空间**。顶部四个 KPI 标明「放心清理 / 谨慎清理 / Skill 目录 / 不可删除」的量级。点分类卡片上的 **清理** 或 **谨慎清理** 按钮：日志 / 缓存类直接清理；会话类会弹窗让你勾选要删的会话、先生成会话总结备份，再统一移入回收站。

**问：我想翻看之前和某个 Skill 的某段对话？**
→ 进入 **对话**，默认在「WorkBuddy 会话」标签：左侧按项目展开会话树，点击任一会话，右侧即显示渲染后的完整消息；在顶部搜索框输入关键词，可跨所有历史会话检索，结果高亮命中位置，点结果直接跳到对应会话。

**问：我想和某个 Skill 直接聊天提问？**
→ 在 **对话** 里切到「与 Skill 对话」标签，从下方 Skill 列表点选一个，输入框里打字发送即可。若提示未配置 LLM，请先到第 4.7 步的设置里填写。

**问：我有个想法，想自动生成一个 Skill？**
→ 进入 **Skill开发**，点右上角 **新建 Skill**，在弹窗里描述你的需求（目标 / 输入 / 输出 / 约束等），系统自动检索零件库、拼装、生成可安装 Skill；生成后点 **安装** 即装入 WorkBuddy。生成记录里可看到它用了哪些零件、当前状态（已生成 / 已安装）。

**问：我想看清某个 Skill 到底由哪些零件构成？**
→ 进入 **组件管理** 浏览零件货架（6 大类），或进入 **任务拆解** 看生成 Skill 拆解回填的零件进度；点任意零件卡片可看详情与反向关联（被哪些 Skill 使用、哪些同源）。

**问：界面太亮 / 太暗，怎么换主题？**
→ 进入 **设置 → 外观**，点 **🌙 暗色** 或 **☀️ 亮色** 即时切换。

**问：与 Skill 对话时提示「尚未配置 LLM」？**
→ 进入 **设置 → LLM**，填好 Base URL / API Key / Model，点 **💾 保存配置**（可选先点 **🔌 测试连接** 验证），回到对话页即可聊天。

### 4.2 界面操作速览

- **打开控制台**：在 WorkBuddy 中调用「零件杂货铺」，浏览器即载入控制台首页（概览驾驶舱）。
- **切换板块**：点左侧导航的任意一项（概览 / Skill 管理 / 存储空间 / Skill开发 / 任务拆解 / 组件管理 / 对话 / 任务 / 异常日志 / 设置）。
- **筛选与搜索**：在 Skill 管理页顶部用筛选标签 + 搜索框缩小范围；输入即实时过滤，并自动回到第 1 页。
- **翻页**：列表类页面（Skill 管理、Skill开发）底部有「⏮ 首页 / ◀ 上一页 / 第 x / n 页 / 下一页 ▶ / 末页 ⏭」，只有一页时自动隐藏。
- **升级 Skill**：在 Skill 管理页点某行的 **升级**，控制台联网校验 SkillHub / 仓库是否有新版本。
- **删除 Skill**：在 Skill 管理页点某行的 **删除**，按弹窗提示确认，系统做备份后再移除。
- **检索历史对话**：在对话页「WorkBuddy 会话」标签顶部搜索框输入关键词回车，结果列表点击即定位。
- **新建并安装 Skill**：在 Skill开发页点 **新建 Skill** → 填需求 → 等生成 → 点 **安装**。

## 五、项目架构介绍

**整体形态**：纯文件 + 脚本 + 本地后端，无外部数据库、无云端依赖。

```
<skill-root>/
├── SKILL.md                  # 本文件（Skill 元信息与说明）
├── README.md                 # 产品使用手册
├── library/                  # 零件库（持久数据源，按 6 大类子目录组织）
│   ├── requirements/         # 需求分析
│   ├── design/               # 方案设计
│   ├── coding/               # 代码开发
│   ├── testing/              # 测试用例
│   ├── tools/                # 依赖工具
│   └── reference/            # 参考文档
├── generations/              # 每次组装产物（manifest + SKILL.md）
├── tests/                    # 全量自动化测试（隔离临时目录，不触碰真实环境）
├── assets/                   # 控制台静态资源（如头像）
├── scripts/
│   ├── workshop.py           # 核心库：检索 / 冲突二选一 / 依赖补全 / 反向关联 / 组装
│   ├── dismantle.py          # 解析 SKILL.md 为候选零件
│   ├── assemble.py           # 给定需求输出选用零件
│   ├── seed_initial_parts.py # 初始化种子零件
│   ├── scan_console.py       # 扫描本机 WorkBuddy，生成控制台数据快照
│   ├── server.py             # 管控台本地后端（提供扫描快照、Skill 增删、对话代理、搜索、生成 / 安装等接口）
│   └── generate_shop.py      # 早期解剖图快照（默认不再生成）
├── console.html              # 管控台前端（单文件、零依赖，可离线打开）
├── console_data.json         # 本机扫描快照（供后端与离线视图使用）
├── console_config.json       # 本地配置（含 LLM / SkillHub 密钥，不入库）
└── start_console.bat / .ps1  # 启动脚本
```

**数据 / 视图解耦**：`library/` 与 `generations/` 是唯一持久数据源；`console.html` 只是视图，由扫描程序把最新数据注入快照，重新生成不会丢失任何零件或生成记录。`shop.html` 为早期解剖图视图，功能已并入控制台「组件管理 / Skill开发 / 任务拆解」，默认不再生成。

**前后端协作**：前端 `console.html` 通过浏览器请求本地后端拿到实时数据（Skill 增删、对话代理、历史检索、生成安装等）；离线打开 `console.html` 时则读取已内联的快照，部分需后端的实时功能会受限。

**积累闭环**：组装生成新 Skill → 自动拆解回零件库（`source_type=auto_generated`）→ 零件库更丰富 → 下次组装检索命中更多。

**质量保障**：`tests/` 含 pytest 全量测试，覆盖零件工坊、控制台接口与端到端集成；测试使用隔离的临时目录，不会扫描或改动你真实的 WorkBuddy 环境。

## 六、设置页面 —— 每项配置说明

进入 **设置** 板块，自上而下共五组配置：

### 1. 外观（🎨）
- **暗色 / 亮色**：二选一切换界面主题，点击即时生效，选择保存在浏览器本地。
- 用途：适配你当前的系统环境或个人观感偏好。

### 2. 控制台后端（🖥️）
- **后端地址**：控制台前端访问后端 API 所用的地址，默认指向本机默认端口。仅当后端不在默认地址（例如改过端口）时才需要改。
- 该值仅保存在浏览器本地，**不写入任何文件**；若改错导致连不上后端，把地址恢复为默认或留空即可。

### 3. 开发环境（🧰）
- **扫描根目录**：当前被扫描的 WorkBuddy 家目录，由扫描程序自动识别，仅作展示、不可编辑。
- **Python 路径**：后端运行所用的解释器路径，自动识别，仅作展示、不可编辑。
- 这两项用于让你确认控制台正在扫描正确的环境，无需手动干预。

### 4. LLM（🤖）
用于「对话 → 与 Skill 对话」页与 Skill 实时交流。
- **Base URL**：大模型接口地址（OpenAI 兼容格式）。
- **API Key**：该接口对应的密钥。
- **Model**：模型名称（如 `gpt-4o` 等）。
- **🔌 测试连接**：保存前后都可以点，向该接口发一条测试消息验证连通性与密钥是否有效。
- **💾 保存配置**：把以上三项写入本地 `console_config.json`，同时缓存到浏览器供对话页使用。未配置时，与 Skill 对话会提示「尚未配置 LLM」。

### 5. SkillHub（🛒）
用于「Skill 管理」的升级校验与版本检查。
- **SkillHub Base URL**：检查已装 Skill 是否有新版本的接口地址。
- **API Key（可选）**：公开接口通常无需填写；私有部署时才填。
- 保存后，Skill 管理页的「升级」会据此联网比对版本。

> 注：LLM 与 SkillHub 的密钥保存在本地配置文件中（不入库、不随 Skill 分发）；后端地址仅存于浏览器本地。请妥善保管本地配置文件。

---

## 七、调用后如何行动（给 agent 的操作指引）

- 用户提到「打开控制台 / 管理 Skill / 清理存储 / 看对话 / 零件库 / 杂货铺 / 看我的 Agent 构成」等意图时，进入本 Skill。
- **默认动作**：启动本地后端并打开控制台首页（`console.html`），进入「概览」驾驶舱。这是本 Skill 的主交付物。
- 仅当用户明确要「组装 / 拆解」Skill 时，走多轮澄清 → 检索组装 → 生成 → 自动回填流程，最终在控制台「Skill开发 / 任务拆解 / 组件管理」查看结果。
- 所有写操作作用于 `library/` 与 `generations/` 数据；控制台视图由扫描程序注入数据，不应直接手改 `console.html`。
- 路径一律用相对 `<skill-root>` 表达，避免在文档或产物里写入本机绝对路径。
