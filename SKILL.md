---
name: agent-grocery-workshop
slug: workbuddy
displayName: WorkBuddy运维控制台
display_name: WorkBuddy运维控制台
description: 你装了一堆 Skill，却总在不断踩坑：到底哪个该升级、哪个有安全风险、哪个其实是重复的，全靠肉眼翻；WorkBuddy 存储悄悄涨到几个 G，想清又不敢乱删；半年前和某个 Skill 的对话翻不到；想报 Bug 找不到入口；从零手搓一个新 Skill 更要写一堆文件、配一堆东西。WorkBuddy 运维控制台把这些痛点一次解决：🚀 特色功能——支持自动 Skill 开发并发布 SkillHub，凭一句话需求由零件库自动检索、拼装成可安装 Skill 并一键上架，无需手写发布流程；🩺 Skill 健康度自动检测更新 / 安全风险 / 功能重叠，一键升级，删除先 AI 总结备份再进回收站、可恢复；🧹 存储按日志 / 会话 / 缓存分类，一键清理，会话类先生成总结备份再清；🔎 历史对话跨所有会话全文检索并高亮，点结果即定位；🎫 内置飞书工单，提交 / 按号查询 / 跟踪处理状态；📤 一键生成只读分享页，把 Agent 资产全景发给别人看，对方无需 WorkBuddy 环境。当用户说「打开控制台 / 管理 Skill / 清理存储 / 看对话 / 零件库 / 提交工单 / 生成分享链接 / 自动帮我生成一个 Skill / 发布到 SkillHub」等意图时唤起；安装后通过 WorkBuddy「与 Skill 对话」入口选择本 Skill 即可在浏览器打开控制台。
description_zh: 装了一堆 Skill 却管不过来？WorkBuddy 运维控制台一站式解决：自动拼装并发布 Skill 到 SkillHub、Skill 健康度检测与一键升级 / 安全删除、存储清理、历史对话全文检索、飞书工单闭环，还能一键生成只读分享页。
description_en: Drowning in installed Skills? WorkBuddy Ops Console fixes it all in one place — auto-assemble and publish Skills to SkillHub, health-check / upgrade / safe-delete, storage cleanup, full-text conversation search, Feishu ticketing, and one-click read-only share pages.
tags:
  - workbuddy
  - console
  - skill-manager
  - 管控台
  - 零件库
  - 工单管理
  - 静态分享页
  - skill-assembly
  - 运维控制台
  - skill发布
summary: 装了一堆 Skill 却管不过来？WorkBuddy 运维控制台一站式解决：自动拼装并发布 Skill 到 SkillHub、Skill 健康度检测与一键升级 / 安全删除、存储清理、历史对话全文检索、飞书工单闭环，还能一键生成只读分享页。
homepage: https://github.com/Dillon-Xue/agent-grocery-workshop
license: MIT
version: 1.0.0
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
  - AskUserQuestion
visibility: public
agent_created: true
---

# WorkBuddy运维控制台

你是一个**本地化的 WorkBuddy Agent 工作台**。它把两件原本分散的事合到一处：一张零依赖的浏览器控制台（管控台），统一管理本机 WorkBuddy 环境；以及一台零件引擎，把已有 Skill / 文档拆成可复用「零件」，再按模糊需求自动检索、冲突二选一、依赖补全，拼装成新 Skill。

## 何时使用（触发场景）

命中以下任一意图即进入本 Skill：

- 「打开控制台 / 打开 WorkBuddy 控制台」—— 打开浏览器管控台首页（概览驾驶舱）。
- 「管理我的 Skill / 升级某个 Skill / 删除没用的 Skill」—— 进入 Skill 管理。
- 「清理 WorkBuddy 存储 / 看看占了多大空间」—— 进入空间管理清理。
- 「看我的 Agent 构成 / 零件库里有什么 / 拆开某个 Skill 看看」—— 进入组件管理 / 拆解任务。
- 「提交一个工单 / 看看我报过的 Bug 处理得怎样了」—— 进入工单管理。
- 「把控制台分享给别人看 / 生成只读分享链接」—— 构建静态分享页（见下文「静态分享页」）。
- 「翻一下之前和某个 Skill 的对话」—— 进入对话检索。
- 「我想自动生成一个 Skill / 凭一个想法造个新能力」—— 进入 Skill开发 走组装流程。

**触发词参考**：打开控制台、管理 Skill、清理存储、看对话、零件库、Agent 构成、生成 Skill、组装 Skill、提交工单、分享链接、workbuddy console。

## 默认动作 / 工作流程

**默认动作（用户未明确要求组装时）**：

1. 启动本地后端（轻量 HTTP 服务）并打开 `console.html`，进入「概览」驾驶舱。这是本 Skill 的主交付物。
2. 概览页一屏展示：已装 Skill 数、Token 偏胖数、近 7/30 天对话数、安全警告数、功能重叠对数、健康度环形图、存储分布、Token 消耗 TOP5，以及「功能重叠 / 安全风险 / 更新检查」三栏。
3. 仅当用户明确要「组装 / 拆解」Skill 时，走多轮澄清 → 检索零件 → 冲突二选一 → 依赖补全 → 拼装生成 → 自动回填零件库，最终在控制台「Skill开发 / 拆解任务 / 组件管理」查看结果。

## 功能概览（控制台板块）

控制台左侧导航共 10 个板块：

| 板块 | 说明 |
|---|---|
| **概览** | Agent 资产全景驾驶舱：KPI、健康度环形图、存储分布、Token 消耗 TOP5，内嵌「功能重叠 / 安全风险 / 更新检查」三栏。 |
| **Skill 管理** | 已装 Skill 明细表：健康度 / 来源 / 安装日期 / 未使用天数 / 使用次数 / Token / 体积 / 安全等级 / 状态；支持筛选、搜索、翻页（每页 10 行）、一键升级、页面内删除。 |
| **空间管理** | 占用按「日志 / 会话 / 缓存」分类，按风险分四级；提供清理，会话类先生成 AI 总结备份再移入回收站。 |
| **Skill开发** | 查看历史生成记录；点「新建 Skill」描述需求 → 自动组装 → 生成可安装 Skill → 一键「安装」到 WorkBuddy。 |
| **拆解任务** | 已生成 Skill 自动拆解回填零件的进度看板（组装 → 拆解 → 回填闭环）。 |
| **组件管理** | 零件货架：6 大类（需求分析 / 方案设计 / 代码开发 / 测试用例 / 依赖工具 / 参考文档）浏览，卡片 / 列表双视图，支持翻页与反向关联。 |
| **对话** | ①「WorkBuddy 会话」—— 按项目 / 会话树浏览，搜索框跨历史全文检索并高亮；②「与 Skill 对话」—— 选已装 Skill 实时问答（需先在设置配置 LLM）。 |
| **工单管理** | 基于飞书多维表格的工单系统：提交工单、按工单号查询、列表筛选与分页；本地控制台可读写，静态分享页为只读快照。 |
| **异常日志** | 运行异常与错误采样，便于排查。 |
| **设置** | 主题、后端地址、开发环境信息、LLM、SkillHub 五项配置。 |

## 使用说明（全部为界面操作，无需命令行）

### 常见问答场景

**问：我一共装了多少个 Skill，有没有安全风险？**
→ 打开控制台，进入 **概览**。KPI 直接显示「已装 Skill 数 / Token 偏胖数 / 安全警告数 / 功能重叠对数」；「🛡️ 安全风险」卡片列出高风险 Skill 及具体发现；「🧬 功能重叠检测」列出功能重复的 Skill 对与重叠百分比。

**问：某个 Skill 不想要了，怎么安全删除？**
→ 进入 **Skill 管理**，找到目标行，点右侧 **删除**。系统先生成 AI 总结备份，再把目录移入系统回收站（可恢复），不直接永久抹除。

**问：WorkBuddy 占了很多空间，怎么清理？**
→ 进入 **空间管理**，顶部标明「放心清理 / 谨慎清理 / Skill 目录 / 不可删除」量级。点分类卡片的 **清理**：日志 / 缓存直接清理；会话类先勾选、生成总结备份，再统一移入回收站。

**问：想翻看之前和某个 Skill 的对话？**
→ 进入 **对话 → WorkBuddy 会话** 标签：左侧按项目展开会话树，点会话看渲染消息；顶部搜索框输入关键词回车，跨所有历史会话检索并高亮命中，点结果即定位。

**问：想和某个 Skill 直接聊天提问？**
→ 进入 **对话 → 与 Skill 对话** 标签，从 Skill 列表点选一个，输入框打字发送。若提示未配置 LLM，先到 **设置 → LLM** 填写。

**问：我有个想法，想自动生成一个 Skill？**
→ 进入 **Skill开发**，点 **新建 Skill**，描述需求（目标 / 输入 / 输出 / 约束），系统自动检索零件、拼装、生成可安装 Skill；生成后点 **安装** 即装入 WorkBuddy。

**问：我想提交一个工单 / Bug？**
→ 进入 **工单管理**，点 **+ 新建工单**，填「用户 / 联系方式 / 类型 / 问题描述 / 期望结果」提交；系统生成 `FB-` 前缀工单号（如 `FB-0032`）。

**问：怎么查我报过的工单处理得怎样了？**
→ 进入 **工单管理**，在顶部查询框输入工单号（大小写 / `fb-7` / 纯数字 `7` 均可容错）回车，定位到对应工单，查看状态列与处理备注。

**问：把控制台分享给别人，对方能提交工单吗？**
→ 静态分享页是只读快照，默认不能提交；需在本地控制台提交，或部署云端工单代理并在构建时注入 `TICKET_API_BASE` 后重新生成分享页。

**问：界面太亮 / 太暗？**
→ 进入 **设置 → 外观**，点 **🌙 暗色** 或 **☀️ 亮色** 即时切换。

### 界面操作速览

- **打开控制台**：在 WorkBuddy 调用本 Skill，浏览器即载入首页（概览）。
- **切换板块**：点左侧导航任一项。
- **筛选 / 搜索**：Skill 管理页顶部用筛选标签 + 搜索框实时过滤，自动回到第 1 页。
- **翻页**：列表类页面（Skill 管理、Skill开发、组件管理、工单管理）底部有「⏮ 首页 / ◀ 上一页 / 第 x / n 页 / 下一页 ▶ / 末页 ⏭」。
- **升级 / 删除 Skill**：Skill 管理页点对应按钮，按提示确认。
- **检索历史对话**：对话页「WorkBuddy 会话」标签顶部搜索框输入关键词回车。
- **提交工单**：工单管理页点 **+ 新建工单** → 填表单 → 提交，得到 `FB-` 工单号。
- **新建并安装 Skill**：Skill开发页点 **新建 Skill** → 填需求 → 等生成 → 点 **安装**。

## 设置页 —— 每项配置说明

进入 **设置** 板块，自上而下五组：

1. **外观（🎨）**：暗色 / 亮色二选一，即时生效，保存在浏览器本地。
2. **控制台后端（🖥️）**：后端地址，默认指向本机默认端口；仅当端口变更时才改。仅存浏览器本地，不写文件，改错恢复默认或留空即可。
3. **开发环境（🧰）**：扫描根目录 / Python 路径，由扫描程序自动识别，仅展示、不可编辑。
4. **LLM（🤖）**：Base URL / API Key / Model；可先点 **🔌 测试连接** 再点 **💾 保存配置**（写入本地 `console_config.json`）。未配置时「与 Skill 对话」会提示「尚未配置 LLM」。
5. **SkillHub（🛒）**：SkillHub Base URL / 可选 API Key，用于 Skill 管理页「升级」联网校验版本。

> 密钥保存在本地配置文件（不入库、不随 Skill 分发）；后端地址仅存浏览器本地。请妥善保管本地配置。

## 静态分享页（只读）

控制台可构建为单文件只读分享页，把资产全景发给他人查看，而无需对方拥有 WorkBuddy 环境或后端。

- **构建**：运行 `scripts/build_static.py`，注入 `window.STATIC_MODE=true`、把最新工单快照写入 `window.TICKET_SNAPSHOT`，并复制 `console_data.json` 与 `assets/`，产物在 `deploy_static/index.html`。
- **部署**：`deploy_static/` 为纯静态文件，可直接托管到任意静态空间（如 CloudStudio / 对象存储），或本地双击打开。
- **只读限制**：分享页不连后端，列表 / 工单均为快照，不可写、不可提交。若有代理 URL，构建时注入 `window.TICKET_API_BASE` 即可让公开访客提交工单。
- **忽略项**：`deploy_static/`、`console_data.json`、`.env` 已在 `.gitignore` 中，不入库。

## 给 agent 的操作指引

- 用户提到「打开控制台 / 管理 Skill / 清理存储 / 看对话 / 零件库 / 看我的 Agent 构成 / 提交工单 / 生成分享链接」等意图时，进入本 Skill。
- **默认动作**：启动本地后端并打开控制台首页（概览）。这是主交付物。
- 仅当用户明确要「组装 / 拆解」Skill 时，走多轮澄清 → 检索组装 → 生成 → 自动回填，最终在控制台「Skill开发 / 拆解任务 / 组件管理」查看结果。
- 工单数据持久化在飞书多维表格（由 `scripts/feishu_ticket.py` 读写），不落在本地文件；如需公开分享页可提交工单，需在构建时注入云端代理 `TICKET_API_BASE`。
- 静态分享页由 `scripts/build_static.py` 生成到 `deploy_static/`，不应手改其产物。
- 所有写操作作用于 `library/` 与 `generations/` 数据；控制台视图由扫描程序注入，不应直接手改 `console.html`。
- 文档与产物中路径一律用相对 `<skill-root>` 表达，不写入本机绝对路径或仓库地址。
